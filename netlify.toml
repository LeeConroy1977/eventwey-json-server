[build]
  command = ""  # No build command is needed for a JSON server
  publish = ""  # Leave this empty because you are not serving static files
  functions = "functions-build" # Directory where Netlify Functions are built

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200