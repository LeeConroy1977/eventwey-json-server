{
  "name": "json-server-deploy",
  "version": "1.0.0",
  "description": "Simple JSON server for testing",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json",
    "build": "netlify-lambda build functions"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "netlify-lambda": "^2.0.16"
  }
}
